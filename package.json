{
  "name": "telegram-rabbitmq-bot",
  "version": "1.0.0",
  "description": "Telegram bot that sends messages to RabbitMQ and reads messages from RabbitMQ",
  "scripts": {
    "start:student": "node StudentBot.js",
    "start:teacher": "node TeacherBot.js"
  },
  "dependencies": {
    "amqplib": "^0.10.0",
    "dotenv": "^16.0.3",
    "telegraf": "^4.0.0",
    "amqp-connection-manager": "^3.2.0"
  }
}
